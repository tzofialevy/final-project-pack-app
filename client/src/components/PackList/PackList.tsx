import React, { FC } from 'react';
import './PackList.scss';

interface PackListProps {}

const PackList: FC<PackListProps> = () => (
  <div className="PackList">
    PackList Component
  </div>
);

export default PackList;
